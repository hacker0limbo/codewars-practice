// 给定两个字符串, 返回第二个字符串在第一个字符串中的个数
var countCombinations = function(string, key) {
    var result = 0
    for (var i = 0; i < string.length; i++) {
        var letter = string[i]
        if (key.includes(letter)) {
            
        }
    }

}
